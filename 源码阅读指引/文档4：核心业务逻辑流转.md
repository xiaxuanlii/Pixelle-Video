# 核心业务逻辑流转

**目标读者**：需要修改业务逻辑的开发者。

## 核心链路分析
**“异步视频生成流水线”** (通过 `StandardPipeline` 驱动)。
流水线的核心逻辑是将用户的初始 Prompt（甚至只是一句话），逐渐膨胀为一份详尽的结构化 `Storyboard`（分镜脚本），然后再将脚本逐帧物化为图像和音频，最后压制出片。

## 可视化时序图
```mermaid
sequenceDiagram
    autonumber
    actor Client
    participant API as FastAPI (Router)
    participant Task as Task Manager
    participant Pipe as StandardPipeline
    participant LLM as LLM Service
    participant Asset as TTS/Image Service
    participant FFmpeg as Video Service

    Client->>API: POST /api/video/generate/async
    API->>Task: Submit Video Task
    Task-->>API: Return task_id
    API-->>Client: Task Accepted
    
    Task->>Pipe: run_pipeline()
    Pipe->>LLM: generate_content() (生成文案脚本)
    LLM-->>Pipe: 剧本结构
    Pipe->>LLM: plan_visuals() (规划画面 Prompt)
    LLM-->>Pipe: 画面提示词列表
    
    Pipe->>Pipe: initialize_storyboard() (生成分镜表)
    
    par 并发生成各分镜素材
        Pipe->>Asset: produce_assets() (TTS配音 + AI画图)
        Asset-->>Pipe: 音频/图片文件路径
    end
    
    Pipe->>FFmpeg: post_production() (音视频合成/添加BGM)
    FFmpeg-->>Pipe: 最终 MP4 路径
    Pipe-->>Task: 任务完成，写入结果
    
    Client->>API: GET /api/tasks/{task_id}
    API-->>Client: Video Ready & URL
```

## 数据流向
在整个流程中，`PipelineContext` 贯穿始终。初始只包含 `user_prompt` 和基础配置。随后：
1. LLM 阶段为其注入 `script`（文案）和 `title`。
2. 生成阶段创建并填充 `storyboard` 对象，其中包含了 `StoryboardFrame`（单帧）。
3. Asset 阶段为每个 `Frame` 填入本地生成的 `image_path` 和 `audio_path`。
4. 最终输出包装为 `VideoGenerationResult` 提供给 API 层返回。